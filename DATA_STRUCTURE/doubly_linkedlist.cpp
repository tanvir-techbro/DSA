#include <cstddef>
#include <iostream>
using namespace std;

// Doubly linked list
struct node {
        double data;
        node *prev;
        node *next;

        // Constructor to initialize default node
        node(int value) {
                data = value;
                prev = NULL;
                next = NULL;
        }
};

int main(int argc, char *argv[]) {

        //

        return 0;
}
